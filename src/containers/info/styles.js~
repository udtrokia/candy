

const styles = theme => ({
  root: theme.mixins.gutters({
      paddingTop: 16,
      paddingBottom: 16,
      margin: theme.spacing.unit * 3,
      marginBottom: 0
  }),
    ctn:{
	display:'flex',
	flex:1,
	flexDirection:'column'
    },
    btn:{
	display:'flex',
	margin: theme.spacing.unit * 3
    }
});

export default styles
